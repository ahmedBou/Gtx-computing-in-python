def multiply(num1, num2):
    product = num1 * num2
    return product
# Assume that only the code shown above is present in our program (nothing before or after).
# If we run the code, which lines of code are executed?

# line 1
